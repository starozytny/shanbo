{% extends 'app/pages/security/template.html.twig' %}

{% block title %}Connexion à son espace{% endblock %}

{% block main_content %}
    <div class="main-content">
        <h1>Se connecter</h1>

        <form action="{{ path('app_login') }}" method="post" class="form-login">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

           <div class="line">
               <label for="inputUsername">Nom utilisateur</label>
               <input type="text" value="{{ last_username }}" name="_username" id="inputUsername" required autofocus>
           </div>
            <div class="line">
                <label for="inputPassword">Mot de passe</label>
                <div class="see-password">
                    <input type="password" name="_password" id="inputPassword" required>
                    <span class="icon-vision btn-see-password"></span>
                </div>
            </div>

            <input type="hidden" name="_csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >

            <input type="hidden" name="_target_path" value="{{ path('app_logged') }}"/>

            <div class="line">
                <div id="forget"></div>
            </div>

            <div class="form-button">
                <button class="btn btn-primary" type="submit">
                    Se connecter
                </button>
            </div>
        </form>
    </div>
{% endblock %}